# Port for the API server (default: 3000)
PORT=3000

# Secret for JWT tokens - use a long random string in production
JWT_SECRET=your-secret-key-change-in-production

# Optional: directory for data files (default: backend/data)
# DB_PATH=./data
