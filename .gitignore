# Dependencies
node_modules/

# Secrets (JWT, SMTP) — never commit
backend/.env
.env

# Data is NOT ignored so backend/data/ can be stored on GitHub
# If you want to stop tracking data later, add: backend/data/
